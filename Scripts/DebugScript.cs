using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        Pathfinding bloup = new Pathfinding(10,10,false);
        bloup.grid[0,5].isWalkable=false;
        List<PathNode> liste = bloup.FindPath(0,0,0,9);
        foreach (PathNode node in liste)
        {
            Debug.Log(node);
        }
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
